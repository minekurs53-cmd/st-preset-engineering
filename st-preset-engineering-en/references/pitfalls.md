# pitfalls.md — Lesson Library (every entry actually happened)

> **When to read**: when planning large tasks; when confused.
> **Format**: lesson → how it happened (real case) → the structural defense in this skill. **These are not theoretical risks; they are tuition already paid.**

| # | Lesson | Real case | Structural defense |
|---|---|---|---|
| L1 | **Self-serving documentation**: writing docs about the work replaces the work; needing "docs about managing docs" = documentation surplus | In one real project, nearly a third of all sessions went to maintaining consistency of a documentation corpus running to hundreds of thousands of characters | SKILL.md hard cap ≤300 lines; routing table controls pipeline weight; drift self-check question 1 |
| L2 | **Self-verification**: imagined models + sub-agent roleplay + self-written asserters, conclusions passed on as "verified" | Three consecutive "verification" sessions were all invalid, overturned by a later session | Discipline 2; L4 externalized to the user; assertion applicability boundaries mandatory |
| L3 | **State loss across sessions**: correction counters stuck at old values, SOPs treated as verified, "blocked" while the source code sat on the local disk — the next session starts on false premises | Happened 3 times, always discovered only by the next session | S6's close-out trio + 30-second self-check |
| L4 | **Focus drift**: sliding from "usable by the user" toward "process rigor / tooling completeness", pulled back by the user twice | The user said it outright, twice: "right direction, wrong focus"; "feels like a loop" | Drift self-check question 4; the routing table hard-codes the minimal correct path |
| L4b | **Control-experiment inertia**: "get baseline data first" becomes a procrastination excuse; a fix backed by measured direction should run in parallel with effect comparison | After shipping v2, defaulted to waiting for the user's 10 control turns — the user pointed out "the original preset fluctuates too" | After S3, branch immediately: fixes supported by data → S4; genuine open questions → unverified list |
| L5 | **Missing control group**: with only one data set, effects cannot be attributed (preset problem or model problem?) | Ten output-side turns yielded only the user's current configuration | The test protocol includes comparison baselines; grab a cheap control group whenever possible |
| L5b | **Acceptance assertions not calibrated first**: idealized "byte-identical" is unreachable on derived artifacts (placeholder shifts); the first run always fails and reworks | Assembly reconciliation took two iterations to pass | verification.md hard-codes the normalization step |
| L6 | **Distorting-field warning too late**: a known trap (the `enabled` artifact) not surfaced early in tool output keeps biting others | A community tool's fallback rules produced wrong results for that family | The S1 dossier lists "distorting fields" as a fixed check item |
| L6b | **Emoji anchor drift**: emoji in entry names carry variation selectors; cross-script matching fails | The same entry name written differently in two scripts; first-run assertion failure | surgery.md: all programmatic locating via numeric/ASCII substrings |
| L7 | **Over-strict safety has costs too**: a compliance tool refusing to write out mapping tables containing original text is correct, but blocking even temporary analysis channels pushes people to work around it | The classify→apply flow had to be adjusted twice | Red lines state "what is allowed", not only "what is forbidden"; workaround needs go through the formal channel — never open private ones |
| L8 | **The source code is right there**: the most important question stays "blocked, need leads" while the answer sits 15 minutes away in local source | The same improvement item sat marked "blocked" for three consecutive sessions | Any "what does ST actually do" question: first action is reading local ST source (`public/scripts/` — macros / openai / regex engine / PromptManager are proven forensics points), evidence rank equal to official docs |
| L9 | **Legend assumption too strong**: assuming the author wrote group semantics in description entries — real presets may have no description entries, legends embedded in entry names, or semantics as in-name annotations ("(pick one)") — the first extraction pass missed that third form and with it 47 rule-evidence hits | Three-preset field test: one had no description entries at all; one's rules lived entirely in in-name annotations (first pass missed the clusters; two passes to converge) | autopsy §4: check all three rule-evidence forms; legend retrieval spells out all three forms (body / name / none) with downgrade paths |
| L10 | **Built-in recognition rules untrustworthy**: the tool's first version recognized one zone style and missed all 22 headings of the next preset; the sample notation system (❗❔) matched none of the three real presets | Three-preset field test: A = paired boundaries, B = emoji+category names, C = "----X" headings — none used the sample system | Zone/notation recognition fully data-driven (profile regexes over built-ins); the contract card's examples are only examples — real styles go into the profile |
| L11 | **Literal extraction bites both ways**: regex-side extraction recognizes only `</?tag>` shapes, missing consumers hidden in syntax patterns (false negative); reference scanning floods on short names / built-in slot words (measured: 6 spot checks, all false) | Three-preset field test: `<thinking>`'s consumer sat in a regex find, matrix showed "one-sided"; reference-scan output was largely unusable | False negatives: targeted substring recheck for every "one-sided" tag; false positives: Card 9's consumption-semantics criterion + the three identifier forms (autopsy §4) |
| L12 | **Non-preset input read as an "empty preset"**: with no input-type gate, a lorebook-style input (no top-level prompts key) produces a "well-formed but empty" report with exit 0 — misread as "this preset is empty" | Measured: one preset was a lorebook; Card 5 emitted an empty report with exit 0, Card 3 classify was equally silent (entries=0, exit 0); only the probe tool gave a `[!] non-preset structure` hint | Card 5 entry input-type gate: no prompts key / entries is a dict → print an input-recognition notice and refuse an empty report (toolmap Card 5); Diagnosis step 1 confirms the input type first |
| L13 | **Assembly-form dimension missing from the dossier**: the first version recorded only the inline order; absolute-injection entries (`injection_position=1`) and custom sp=true entries were not listed separately — reordering conclusions do not hold for them | Measured: several presets carry 8 absolute injections total (incl. an ON·depth=2 one); some carry agent* sp=true entries; all missed in the first version, patched in later | Card 5 §1 pipeline table annotates the two assembly forms + enabled/depth/inj_order; autopsy §1 reading row adds the column |
| L14 | **Alternation-form tags missed by the contract matrix**: `<(a|b|c)>` is not recognized by `</?tag>`-shape extraction → the matrix wrongly reports "one-sided" | Measured: `<(thinking|suggestions|disclaimer)>` was graded one-sided, actually consumed two-sided; fixed by a targeted substring recheck | Every tag the matrix marks "one-sided" gets one targeted substring recheck (toolmap Card 9 / Diagnosis step 3) |
| L15 | **Stale refs left unreconciled**: the orphan table only checks one direction (prompts→order); the reverse direction — order referencing identifiers that do not exist — has no output slot at all, so hot-upgrade rename residue sails through the whole pipeline | Measured this round: two order blocks each carried 2 old-version identifiers (pc01) while the current entries (pc02) became orphans; all four stale refs surfaced only via an ad-hoc probe — the tool output "3 orphans" gave no hint that anything was wrong on the order side | Card 5 two-way reconciliation: stale-ref list emitted alongside the orphan table; on= column distinguishes orphan/stale; self-test gains a stale-ref sample |
| L16 | **Same-name boundary pairs cannot close**: boundary entries sharing a name at open and close (——X—— appearing twice) fail to close the zone under sequential title labeling; following entries get glued into the previous zone | Measured this round: a 218-entry preset's dominant boundary form is same-name pairs; E14-E30's zone column was glued into the previous zone (cluster conclusions in §3 verified unaffected, but the zone column was locally wrong) | profile-spec/toolmap register "same-name boundary pairs (toggle type)"; when start/end regexes are identical the tool applies "second occurrence of the same name = close"; self-test gains a toggle sample |
| L17 | **Copy layers "listed but not compared" ≠ reconciled**: multiple regex layers listed separately with no automatic set/field diff — "which layer is effective" and "are they in sync" still depend on the human eye | Measured this round: native 22 vs binding layer 16 — 8 only-native, 2 only-copy, same-name find length 30 vs 54 — only obtainable via an ad-hoc script; unreadable from the table | Card 5 §5 copy-layer reconciliation lines (only-native / only-copy / same-name field diffs); autopsy §2's law of three copies routes through that output |

## How to use

- Before planning any task >30 minutes, scan this table and ask: will this task hit one of these?
- If yes → the defense is already in SKILL.md/references; follow it. Defense insufficient → build the defense before starting.
- New lessons: register only what really happened; include "case" and "defense" columns — a lesson without a defense is a lesson not learned.

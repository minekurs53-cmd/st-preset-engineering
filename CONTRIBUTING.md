# 贡献指南

感谢关注 st-preset-engineering！欢迎 Issue 与 PR。

## 提交前必读

这是一个以「安全与隐私」为核心卖点的仓库，以下红线适用于所有贡献内容：

1. **禁止提交真实敏感数据**：预设正文、真实路径、凭据、预设家族的私有命名与内部目录名。示例一律用 `<占位符>` 形式。
2. **新纪律需要案例与防线**：向 `pitfalls.md` 登记新教训时，按既有格式填写「教训 → 真实案例 → 结构性防线」两栏——没有防线的教训等于没记；案例须做脱敏。
3. **中英双语同步**：`st-preset-engineering/` 与 `st-preset-engineering-en/` 内容一一对应。改动任一版本时必须同步另一版本（结构、行号不必逐字对齐，语义必须一致）。
4. **版本元数据同步**：改动 skill 内容时，同步更新两份 SKILL.md 的 `metadata.version` 与 `CHANGELOG.md`（遵循 [语义化版本](https://semver.org/lang/zh-CN/)）。

## Issue

- **Bug 报告**：说明代理平台、触发的请求原文、预期行为与实际行为；涉及预设时附**脱敏后**的最小复现片段。
- **功能建议**：说明目标用户场景，以及为什么现有七步骨架 / profile 机制覆盖不了。
- **提问**：先读 FAQ 与对应 references（`references/` 索引见 README）。

## PR 流程

1. Fork → 建分支（`feat/xxx` 或 `fix/xxx`）
2. 改动保持中英同步（见上）
3. 自查清单：
   - [ ] 不含任何真实路径 / 凭据 / 预设正文（与仓库自身词表审计原则一致）
   - [ ] 中英两版语义一致
   - [ ] CHANGELOG 与 version 已更新
   - [ ] 新增纪律附真实案例与防线
4. PR 描述写清「改了什么 + 为什么 + 影响范围」

## License

提交即表示同意以 [MIT](LICENSE) 许可证发布你的贡献。

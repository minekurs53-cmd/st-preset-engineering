# Changelog

版本号与 `st-preset-engineering/SKILL.md` frontmatter 的 `metadata.version` 保持同步。

## [1.2.0] — 2026-09-14

### 依据

三轮真实预设实测吸收的 skill 侧缺陷（首轮 4 个社区预设 30 条 + 二轮 3 预设 30 条 + 三轮 9 预设 70 条分级发现 / 5 error / 22 warning / 43 info，跨 5 种分区样式、含世界书非预设输入），按"高级别全修、通用低级别择要修"吸收。同时工具层从"调用卡"升级为"能力契约 + 建工具方法论"。教训入库 pitfalls L9–L14。发布前另做一轮**合成 fixture 实效性测试**：按契约卡自建工具（卡1/2/5/9）跑含全部目标缺陷的 fixture，22 项断言全过——逐条验证 v1.2.0 新增能力（输入门禁/绝对注入/容器孤儿/多拷贝/alternation/文本回退/区域分类/引用扫描）真的可被实现并触发。

### 新增

- **工具层从"调用卡"升级为"能力契约 + 建工具方法论"**（`references/toolmap.md` 中英重写）：
  - 每张卡从"参考命令 + 期望输出"扩展为完整契约——**输入 → 判定逻辑 → 输出格式 → 期望输出 → 降级路径 → 自测项**，覆盖 S0–S5 全部工具依赖（源校验 / 词表扫描 / 脱敏管线 / 管线验收 / 结构档案 / 分组注册表 / 离线装配 / mock 端点）
  - 新增"建工具方法论"：`tool_dir` 不可跑时，代理按对应契约卡在本地实现等价工具（Python 3.8+ 标准库），五条纪律——实例数据外置 / 只读写盘纪律 / 输出面最小 / 结论可 diff / 自测先行；先过 fixture 自测再上真实数据，新建工具在交付说明中逐个声明
  - SKILL.md「不重造、不复制」条款改为"工具在跑就照卡调用；不可跑就按契约建、自测、登记"，工具缺失不再阻塞流程
- **toolmap 卡 9 — 引用扫描**（中英）：S3 三方一致与 S4 改名/删条目前的 UI 引用检查——两轮测试均独立判定为必经检查而 skill 原无此卡。含假阳性判据（命中须能解释为消费语义；排除 CSS 数值/正则反向引用/脚本自身标识符/标准槽词）与 `(id)`/`(name)` 两级标注
- **red-lines 规则 8 — 远程资源风险分级**（中英）：静态资源(I) → 只读 API(W) → 可执行代码注入(E) 三级模板 + 只读任务外联纪律（默认不抓取）——实测某预设第三方域名 `injectScript` 无判据可依，属最大自由裁量项
- SKILL.md 第 0 步新增**冷启动四问**（中英）：真值路径 / 任务深度 / 产物去向 / 模型渠道，各带异步默认——实测"重跑会先问什么"高度收敛于这四项
- profile 新增数据驱动字段（中英 schema + 样例注释）：`zone_title_re` / `zone_pair_start_re` / `zone_pair_end_re` / `zone_category_re` / `block_rules`（count_in/mutex）/ `complexity.note` / `extension_hookpoints`
- **卡 5 输入类型门禁**（实测世界书输入被当"空预设"）：无 `prompts` 键 / `entries` 为 dict → 输出输入识别提示并拒绝空报告
- **卡 5 装配形态维度**（实测共 8 条绝对注入 + 部分预设 agent* sp=true 首版全漏）：§1 管道表单独标注绝对注入条目（injection_position=1 + depth + inj_order）与自定义 sp=true 条目
- **孤儿表数据容器区分**（实测某孤儿为 18460 字符 JSON 容器）：标注"容器候选（数据副本，非功能条目）"+"不应接回"警示
- **卡 1 `--files` 单文件/子集清单模式**（三轮实测：9 个源文件散落共享目录，`--dir` 递归会越界扫进他人预设）
- **卡 2 任意文本可扫 + 区域分类**（三轮实测：19 件交付文档 10 份 .md 首跑 ERROR；`--regions` 让"85 命中全落 other=纯结构性"可复跑）
- pitfalls 新增 L9–L14（中英）：图例假设过强 / 内置识别规则不可信 / 字面量提取双向坑 / 非预设输入被当空预设 / 装配形态维度漏登记 / alternation 形标签被契约矩阵漏检
- diagnosis 新增**跨预设共享基建**节（三轮实测两个预设共享同名条目同参、正则族互拷）：多预设同场诊断时登记跨预设互引

### 修复（实测踩中的错误或不自洽）

- **卡 5 自测清单数学矛盾**（两轮复现）：互斥组至少 2 成员，"3 条目"无法成立 → 改 5 条目 fixture，并加嵌套参数/多调用 injectScript 样本
- **red-lines 规则 6 与 ST 源码不符**：isConnection 字段实为条件跳过（UI 开启"预设绑定连接"时应用）→ 表述加前提，处置改为核对勾选态
- **卡 3 与红线的策略矛盾**：quarantine 下"explain 调词表前必跑"与"整卡可跳过"冲突 → quarantine 下 classify/explain 仍可跑（只读、不产敏感产物），过严复核有路径
- **卡 2 词表无规范**（两轮复现）→ 补 schema（text/category 三类别枚举）、起步词表建表指引、结构性/内容性命中区分（ST 内置槽名不构成敏感信号）
- **卡 1 两个实测坑**→ 递归口径写明；新增"清单不得写进被扫描目录"写盘防护（首跑自测即踩中）
- **卡 5 记号体系写死**（三真实预设无一匹配示例体系）→ 六节契约改为记号无关：示例只是示例，工具不得内置记号→语义映射；补多 order 块呈现与生效块推定口径；"读"的判定范围写明三处；新增 NO_STATIC_WRITER 判读模板；孤儿>10 条时三线索分簇
- **卡 5 §6 injectScript 提取缺陷**（实测两次修补）→ 契约写明括号配平提取 + 多调用全量列出 + 截断显式标注
- profile-spec 落盘三选项不覆盖沙盒场景 → 补第四种；骨架期"每条命令可跑"与"先建 profile 后建工具"的时序矛盾 → 写明占位许可
- diagnosis：三方一致扫描契约化（引用卡 9 + 字面量矩阵"单方"标签的定向补证）；补"无真值时的默认出路"（行为类全进未验证清单并逐条写缺什么——两轮均为最高级别缺陷）；报告最小模板补扩展挂点清单节
- autopsy：分区/规则识别样式无关化（四种真实样式 + 规则证据三形态 + 图例检索三形态）；新增 §5 引用扫描（identifier 三类形态与假阳性判据）；复杂度判据失真时按字面判 + note 注记
- verification 头部补"无环境 ≠ 无工具"降级口径；autopsy 收尾检查补引用扫描基线项
- profile-spec 补异步任务默认动作（推定 + 待确认继续，不许猜语义）
- **卡 5 §1 分区/装配的实测扩展回填**（三轮）→ 契约写明 `zone_category_re`（emoji+分类名前缀样式）、正则含捕获组时"捕获组 1 = 区名"语义（━━ 分隔标题）；这两项首版缺失、实测自增字段后回填契约
- **契约矩阵 alternation 盲区**（实测 `<(thinking|suggestions|disclaimer)>` 被判单方实为两方）→ 卡 9 / diagnosis 第 3 步写明"单方"标签一律补定向子串核查
- **卡 5 自测清单补三类新样本**（三轮）→ 绝对注入条目 / 自定义 sp=true 条目 / 世界书形态输入（门禁触发）纳入 fixture 断言
- **多拷贝"生效层判定 + 同步核验"工具化**（实测三拷贝/两层/空绑定三种形态）→ autopsy §4 写明不许人眼逐字节，落可复跑命令或 diff
- **GBK 纪律补 stderr**（实效性测试暴露）：原纪律只写重配 `sys.stdout`——但 `sys.exit("中文错误信息")` 走 stderr，非 UTF-8 模式下（PYTHONUTF8=0）stderr 落 GBK 字节，被上层脚本按 utf-8 捕获即解码失败/乱码、断言全失效（探针实测矩阵："只重配 stdout + PYTHONUTF8=0" 解码失败，"同时重配" 恒正确）→ 纪律改为同时重配 stdout 与 stderr
- **autopsy 分区样式 4 → 5**（三轮实测 HTML 标签成对条目 `<background>…</background>`）→ 明列五种真实样式

### 变更

- SKILL.md「工具层」节、compatibility 行与 references 索引同步改写（中英一致）；README/README.en 工具带表述与 FAQ 改为契约模型
- 版本号 1.1.0 → 1.2.0（中英 SKILL.md metadata 同步）

### 设计边界（不变）

- 配套参考实现脚本仍不随本仓库分发：skill 只约定能力契约，实现由本机按契约卡提供；词表/分区正则/规则集均为实例数据，走 profile 注入

## [1.1.0] — 2026-09-14

### 新增

- 英文版 skill `st-preset-engineering-en/`（SKILL.md + 11 篇 references 全量英译），与中文版骨架、纪律、版本号完全同步
- 英文版 `README.en.md`，与中文 README 内容对齐；两份 README 顶部互设语言切换链接
- `examples/preset-profile.sample.json`：实例 profile 脱敏模板（预设名、家族前缀、内部目录、路径均以 `<…>` 占位符呈现，任何预设家族可直接套用）

### 安全与隐私

- 全库隐私审计与历史重建：移除对私有维护项目（仓库名 / 内部治理文档章节 / 内部目录名 / 实例私有命名）的全部引用，改为平台通用表述；重建后的 git 历史不含任何私有项目关联信息
- SKILL.md 对「受治理仓库」的引用改为通用约定（以目标仓库自身协作文档为准），不指向任何具体私有仓库

### 变更

- 鸣谢收敛至 README 末尾单节；正文与 skill 文件不再提及具体预设家族名

## [1.0.0] — 2026-09-13

首次公开发布。

### 新增

- skill 本体 12 文件：`SKILL.md`（七步骨架 S0–S6 + 步骤路由表 + 三条不可违反纪律 + 复杂度自适应 + 反跑偏自查 + 安全边界声明）与 11 篇 references（red-lines / autopsy / truth-sources / diagnosis / surgery / verification / handover / pitfalls / best-practices / profile-spec / toolmap）
- `examples/`：真实实例 profile 的脱敏副本（含模型分支配对 / 活槽 / 标签契约 / heavy 复杂度分级示例）
- 发布合规：SKILL.md「安全边界声明」节（文件访问 / 网络 / 不做的操作 / 无密钥）与 `license` / `compatibility` / `metadata.version` 元数据
- `README.md`：功能说明、特色安全措施（敏感预设的安全处理与截断风险控制）、快速开始、系统要求、合规对照、FAQ

### 设计边界

- 配套工具带（结构档案 / 脱敏管线 / mock 端点 / 离线装配 / 审计等 Python 工具）**不随本仓库分发**：脱敏管线依赖敏感词表、部分注册表含实例数据；skill 通过 `profile.tool_dir` 引用，缺失时明确报告、不静默跳过
- 对抗类档位语义默认不登记进 profile（条目名自含语义，少登记少敏感面）
